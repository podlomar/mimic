[project]
name = "mimic"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "opencv-python",
    "numpy",
]

[project.scripts]
mimic = "mimic.main:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
